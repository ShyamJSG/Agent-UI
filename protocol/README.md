# Codex app-server protocol types

These TypeScript files were generated from the independently installed Linux `codex-cli` version `0.155.1`.

They are generated artifacts. Do not edit them by hand and do not guess protocol fields in application code.

The generation command used was:

```text
codex app-server generate-ts --out ./protocol --experimental
```

Regenerate them only after intentionally changing the pinned Codex version, then inspect the resulting protocol differences and update `PLAN.md` if the architecture or behavior changes.
